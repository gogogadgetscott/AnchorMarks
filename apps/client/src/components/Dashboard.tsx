import React from "react";

/**
 * Dashboard Component
 * Displays dashboard widgets (rendered by legacy code)
 */
const Dashboard: React.FC = () => {
  return null; // HTML is rendered by legacy bookmarks module
};

export default Dashboard;
