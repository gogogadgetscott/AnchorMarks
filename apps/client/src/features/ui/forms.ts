/**
 * Legacy forms bridge.
 * Auth flow dynamically imports this module after injecting login/register markup.
 * React auth handles interactions now, so this remains a safe no-op shim.
 */
export function initFormListeners(): void {}
